---
trdd-id: B0048A21
title: Supersede the 8-column kanban model with the ratified 14-stage pipeline across COS docs (COS#11 decision a)
column: published
created: 2026-06-20T18:36:34+0200
updated: 2026-06-20T18:55:02+0200
current-owner: cos
assignee: cos
priority: 3
severity: LOW
effort: S
labels: [kanban, governance, docs]
task-type: docs
parent-trdd: null
npt: []
eht: []
blocked-by: []
supersedes: [TRDD-59980a31]
relevant-rules: []
release-via: publish
delivery: direct-push
target-branch: main
test-requirements: [lint]
audit-requirements: []
review-requirements: [code-review]
impacts: []
attempts: 0
last-test-result: pass
implementation-commits: [260bafe]
published-version: 2.20.4
published-at: 2026-06-20T18:55:02+0200
external-refs: ["github.com/Emasoft/ai-maestro-chief-of-staff/issues/11", "github.com/Emasoft/ai-maestro/issues/2", "github.com/Emasoft/ai-maestro/issues/40"]
---

# Supersede the 8-column kanban model with the ratified 14-stage pipeline across COS docs (COS#11 decision a)

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative; supersedes the body)

**What this is:** the doc-correctness pass that retires the **8-column** team-kanban
model (encoded by TRDD-59980a31 / v2.20.0) and replaces it with the **ratified
14-stage TRDD pipeline + the 3 exception lanes**, per the MANAGER's ai-maestro#2
**decision (a)** (COS#11 item D, 2026-06-20): *"the 14-stage set + blocked/failed/
superseded, NOT the 8-col model — NO 14→8 projection."* The MANAGER explicitly
instructed COS to "mark the v2.20.0 8-col encoding superseded."

**WHY now (not held):** documenting the ratified MODEL is independent of the server
backend. The 14-stage set is the stable TRDD `column:` enum and the file-based half
is already live (ai-maestro#40: "the file-based half ships and works — TRDD column:
14-stage pipeline + kanban.py"). Leaving the docs asserting the 8-column model is
"ratified / locked in" was a correctness bug (the MANAGER superseded it). Only the
SERVER-BOARD application (COS calling `kanban-config --set` so the board stores the
columns) stays gated on the #2 backend — that part is unchanged and still held.

**Change set (6 shipped files, one canonical 14-stage lane block byte-identical
across the 4 that carry it):**
- `skills/amcos-prrd-trdd-kanban/SKILL.md` — canonical model section: 8-col +
  projection table → 14-stage 1:1, no mapping table. (single source of truth)
- `docs/FULL_PROJECT_WORKFLOW.md` — "8-column projection" section → "no projection".
- `docs/TEAM_REGISTRY_SPECIFICATION.md` — canonical board → 14-stage.
- `docs/AGENT_OPERATIONS.md` — the one-line board mention → 1:1 mirror.
- `skills/amcos-label-taxonomy/references/kanban-and-label-details.md` — heading +
  in-file TOC + lane list + task-routing examples + verb-status (also fixed the
  doubly-stale "not yet deployed — #36" → "deployed; backend gated on #2").
- `skills/amcos-label-taxonomy/SKILL.md` — description + embedded-TOC anchor synced
  to the new heading (`#kanban-columns-ratified-14-stage-pipeline`).

**Canonical lane block (the SoT — must stay byte-identical wherever repeated):**
`backburner · todo · design · dispatch · dev · testing · ai_review · human_review · complete · publish · published · deploy · live · live_auditing` + exceptions `blocked · failed · superseded`

**NEXT ACTION:** none — SHIPPED v2.20.4 (content commit 260bafe, release 39eb87a,
CPV strict 0/0/0/0, 131 tests green); COS#11 updated. The server-board wire-up
(COS calling `kanban-config --set` with the 14-stage schema at team creation)
remains gated on the ai-maestro#2 backend — that is the only #26 portion still open.

**Load-bearing facts / gotchas:**
- `ai_review` and `human_review` stay DISTINCT lanes (the R26–R40 human gate) — by
  construction in the 14-stage set, no special handling needed.
- The TOC-anchor sync (heading rename → anchor change → in-file TOC + the parent
  SKILL.md embedded TOC) is the CPV-embedded-TOC-sync trap — all 3 synced; verified
  old anchor refs = 0, new = 2.
- TWO grep false-positives are NOT remnants: `performance-metrics.md:117` (a
  blocked-task metric) and `FULL_PROJECT_WORKFLOW.md:100` (the TRDD
  `review-requirements:` *value* `human-review`, not the old lane name).

**SUPERSEDED — do NOT carry forward:**
- ✗ The 8-column lane set `backlog · todo · in-progress · ai-review · human-review ·
  merge-release · done · blocked` and its `column:`→lane projection table — replaced
  by the 14-stage 1:1 mirror above.
- ✗ "the model is locked in" / "MANAGER-ratified 8-column model" — the 8-column model
  was superseded by decision (a).

**Durable artifacts to read before acting:**
- COS#11 item D (MANAGER, 2026-06-20T03:17Z) — the decision-(a) re-scope.
- ai-maestro#40 — the 14-stage-vs-server-backend problem statement.
- TRDD-59980a31 — the superseded predecessor (records the v2.20.0 8-column publish).

## Background

TRDD-59980a31 captured the MANAGER's then-Tier-2 ratification of an 8-column board
model (a simplified projection of the TRDD v2 lifecycle) and shipped it as v2.20.0.
On 2026-06-20 the MANAGER's ai-maestro#2 fork **decision (a)** re-scoped the target
to the **full 14-stage TRDD pipeline + blocked/failed/superseded, with no 14→8
projection** — the projection hid the human-review gate and the publish/deploy
tails. This TRDD executes the MANAGER's "mark the 8-col encoding superseded"
instruction as a doc-correctness pass, leaving the server-board wire-up gated on the
ai-maestro#2 backend (still OPEN).
